/* global Backbone, _ */
var App = App || {};

App.StudentView = App.BaseView.extend({
    defaults: function () {},

    constructor: function StudentView() {
        "use strict";
        App.BaseView.prototype.constructor.apply(this, arguments);
    },

    initialize : function () {}
});

